# @galvanite/tokens

Shared design tokens (color, typography, spacing) for galvanite.io, sourced from the Galvanite brand guidelines. Consumed by `@galvanite/ui` (the shadcn-based component package) once that package is added as a sibling under `packages/`.
